[phases.setup]
nixPkgs = ["php", "composer", "nodejs", "npm"]

[phases.build]
cmds = [
  "composer install",
  "npm ci",
  "npm run build"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=8080"
