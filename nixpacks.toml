[phases.setup]
nixPkgs = ["php82", "nodejs_20", "npm", "php82Packages.composer"]

[phases.build]
cmds = [
  "composer install",
  "npm ci",
  "npm run build"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=8080"
